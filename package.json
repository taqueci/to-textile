{
  "name": "rwe-to-textile",
  "version": "0.10.3",
  "description": "An HTML to Textile converter for Redmine WYSIWYG Editor plugin",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "watchify -s toTextile -o docs/to-textile.js -u jsdom index.js -v",
    "build": "browserify -s toTextile -o dist/to-textile.js index.js -u jsdom && uglifyjs dist/to-textile.js -c -m --comments -o dist/to-textile.min.js && cp dist/*.js docs/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/taqueci/to-textile.git"
  },
  "keywords": [
    "textile",
    "html-converter"
  ],
  "author": "cmroanirgo",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/taqueci/to-textile/issues"
  },
  "homepage": "https://github.com/taqueci",
  "dependencies": {
    "collapse-whitespace": "1.1.2",
    "jsdom": "^27.3.0"
  },
  "devDependencies": {
    "browserify": "^17.0.1",
    "uglify-js": "^3.19.3",
    "watchify": "^4.0.0"
  },
  "xdevDependencies": {
    "qunit": "^0.7.6",
    "saucie": "0.1.3",
    "standard": "^6.0.8",
    "testem": "^0.8.2",
    "watchify": "^2.5.0"
  }
}
